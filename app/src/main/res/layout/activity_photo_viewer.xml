<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/photoPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="top"
        android:gravity="center_vertical"
        android:paddingHorizontal="8dp"
        android:background="#80000000">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Back" />

        <TextView
            android:id="@+id/photoCounter"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="16sp" />

        <ImageButton
            android:id="@+id/deleteButtonTop"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_delete"
            android:scaleType="centerInside"
            android:contentDescription="Delete" />
    </LinearLayout>

    <!-- Floating play/pause button (video only) -->
    <ImageButton
        android:id="@+id/floatingPlayPause"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="start|bottom"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="140dp"
        android:background="#80000000"
        android:src="@drawable/ic_play"
        android:scaleType="centerInside"
        android:contentDescription="Play/Pause"
        android:visibility="gone" />

    <!-- Floating mute button (video only) -->
    <ImageButton
        android:id="@+id/floatingMute"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="end|bottom"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="140dp"
        android:background="#80000000"
        android:src="@drawable/ic_volume_off"
        android:scaleType="centerInside"
        android:contentDescription="Mute"
        android:visibility="gone" />

    <!-- Bottom container -->
    <LinearLayout
        android:id="@+id/bottomContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="#80000000">

        <!-- Video time + scrub bar -->
        <TextView
            android:id="@+id/videoTimeText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:paddingTop="8dp"
            android:visibility="gone" />

        <SeekBar
            android:id="@+id/videoSeekBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:progressDrawable="@drawable/seekbar_track"
            android:thumb="@drawable/seekbar_thumb"
            android:minHeight="4dp"
            android:maxHeight="4dp"
            android:visibility="gone" />

        <!-- Info row -->
        <TextView
            android:id="@+id/photoInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="#CCCCCC"
            android:textSize="13sp"
            android:padding="12dp" />

    </LinearLayout>

</FrameLayout>
